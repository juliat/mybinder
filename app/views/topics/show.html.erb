<%- model_class = @topic.class -%>
<h1><%# on_the_spot_edit @topic, :name %> <%= @topic.name %></h1>


<h2>Goals</h2>
<ol>
<% unless @topic.goals.empty? %>
    <% for goal in @topic.goals.in_order %>
        <li><%# on_the_spot_edit goal, :statement %> <%= goal.statement %></li>
    <% end %>
</ol>
<% else %>
    <p>There aren't any goals for this topic.</p>
<% end %>

<h2>Activities</h2>
<% unless @topic.activities.empty? %>
<ol>
    <% for activity in @topic.activities %>
        <li><%= activity.name %></li>
    <% end %>
</ol>
<% else %>
    <p>There aren't any activities for this topic. <%= link_to "Add one!", new_activity_path(:topic_id => @topic.id ) %></p>
<% end %>

<!--
<div class="form-actions">
  <%= link_to t('.back', :default => t("helpers.links.back")),
              topics_path, :class => 'btn'  %>
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_topic_path(@topic), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              topic_path(@topic),
              :method => 'delete',
              :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')),
              :class => 'btn btn-danger' %>
</div>
-->
