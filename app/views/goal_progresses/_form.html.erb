<%= form_for(@goal_progress) do |f| %>
  <% if @goal_progress.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@goal_progress.errors.count, "error") %> prohibited this goal_progress from being saved:</h2>

      <ul>
      <% @goal_progress.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :goal_id %><br />
    <%= f.number_field :goal_id %>
  </div>
  <div class="field">
    <%= f.label :course_class_id %><br />
    <%= f.number_field :course_class_id %>
  </div>
  <div class="field">
    <%= f.label :completed %><br />
    <%= f.check_box :completed %>
  </div>
  <div class="field">
    <%= f.label :notes %><br />
    <%= f.text_area :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
