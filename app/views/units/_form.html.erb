<%= simple_form_for(@unit) do |f| %>
    <%= f.association :course %>
    <%= f.input :name %>
    <%= f.input :start_date, :order => [:month, :day, :year] %>
    <%= f.input :days %>
    <%= f.submit %>
<% end %>
